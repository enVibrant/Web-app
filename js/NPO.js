$.getJSON("NPO.json", function(result){

result.NPO[0].userName 
result.NPO[1].userName
result.NPO[2].userName

for()
{
  if(result.NPO[i].userName == "ACF")
  {
    
  }
}

  // you can find an item in result which has username as "ACF".
  //

  console.log(result);



});